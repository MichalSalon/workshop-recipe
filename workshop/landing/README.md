# Workshop landing (static fallback)

The canonical homepage is the frontend SPA at **`/`** after deploy.
See `apps/frontend/src/WorkshopHome.tsx`.

Local preview:

```bash
npm run dev -w @deck/frontend
```

Facilitator checklist: [`../FACILITATOR.md`](../FACILITATOR.md).
